type: object
description: |
  Reference to external test data for a benchmark. Exactly one of `s3`, `pvc`, `git`, or `hf` must be set.

  **`s3`** — When the evaluation runs as a **Kubernetes Job**, Eval Hub schedules an
  **init container** on the evaluation job pod before the adapter runs. That init container:

  - Reads **`bucket`**, **`key`**, and **`secret_ref`** from the job spec (same JSON
    mounted into the pod).
  - Uses credentials from the Kubernetes **Secret** named by **`secret_ref`**
    (read-only mount; typical sources include an ODH/RHOAI S3 connection Secret).
  - Lists and downloads all objects under the **`key`** prefix into a shared
    **`emptyDir`** volume at **`/test_data`**, preserving relative paths.
  - Exits; the **adapter container** mounts `/test_data` and reads the files.

  **`pvc`** — The PersistentVolumeClaim is mounted **read-only** directly into the evaluation
  container at **`/test_data`**. No init container is used; the PVC must already contain the
  data and must exist in the same namespace as the evaluation job.

  **`git`** — The specified repository is cloned and checked out at the given ref into
  **`/test_data`** before the adapter runs. The resolved commit SHA is recorded as
  **`resolved_sha`** on this object on the evaluation job for reproducibility.

  **`hf`** — The specified Hugging Face Hub repository is downloaded at the given revision
  into **`/test_data`** before the adapter runs. The resolved commit SHA is recorded as
  **`resolved_sha`** on this object on the evaluation job for reproducibility.

  Do not put credentials in the Eval Hub API request body; only reference an existing
  Secret via **`secret_ref`** (for S3, git, or hf).
oneOf:
  - type: object
    properties:
      s3:
        $ref: ./TestDataRefS3.yaml
      resolved_sha:
        $ref: ./TestDataRefResolvedSHA.yaml
      type:
        $ref: ./TestDataRefType.yaml
    additionalProperties: false
    required:
      - s3
    title: S3
  - type: object
    properties:
      pvc:
        $ref: ./TestDataRefPVC.yaml
      resolved_sha:
        $ref: ./TestDataRefResolvedSHA.yaml
      type:
        $ref: ./TestDataRefType.yaml
    additionalProperties: false
    required:
      - pvc
    title: PVC
  - type: object
    properties:
      git:
        $ref: ./TestDataRefGit.yaml
      resolved_sha:
        $ref: ./TestDataRefResolvedSHA.yaml
      type:
        $ref: ./TestDataRefType.yaml
    additionalProperties: false
    required:
      - git
    title: Git
  - type: object
    properties:
      hf:
        $ref: ./TestDataRefHF.yaml
      resolved_sha:
        $ref: ./TestDataRefResolvedSHA.yaml
      type:
        $ref: ./TestDataRefType.yaml
    additionalProperties: false
    required:
      - hf
    title: HuggingFace

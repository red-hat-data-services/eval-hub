properties:
  op:
    type: string
    enum:
      - replace
      - add
      - remove
  path:
    type: string
  value:
    description: Value to be used in the patch operation
    type:
      - object
      - array
      - string
      - number
      - integer
      - boolean
      - "null"
required:
  - op
  - path

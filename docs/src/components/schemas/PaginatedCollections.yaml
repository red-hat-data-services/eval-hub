properties:
  first:
    $ref: ./PaginationLink.yaml
    description: Link to the first page
  next:
    $ref: ./PaginationLink.yaml
    description: Link to the next page, if available
  limit:
    type: integer
    title: Limit
    description: Page size used for this response
  total_count:
    type: integer
    title: Total Count
    description: Total number of collections
  items:
    items:
      $ref: ./Collection.yaml
    type: array
    title: Items
    description: Collections returned for this page
type: object
required:
  - first
  - limit
  - total_count
title: PaginatedCollections
description: Paginated list response for collection resources.

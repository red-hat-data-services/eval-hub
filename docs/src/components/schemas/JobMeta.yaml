type: object
description: |
  Job-level metadata on status events that is not part of benchmark state.
properties:
  resolved_sha:
    type: string
    description: |
      Resolved content identity for the job's test data (e.g. git commit SHA).
      Server-populated for git test data sources; not accepted on job create.
    examples:
      - a3f9c12e8b4d6f1c2e9a7b3d5f0e8c4a2b6d9e1f
